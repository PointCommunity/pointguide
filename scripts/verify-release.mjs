const base=process.env.VERIFY_BASE_URL??"http://127.0.0.1:3000"; for(const path of ["/api/healthz","/api/readyz"]){const response=await fetch(base+path);if(!response.ok)throw new Error(`${path} returned ${response.status}`);} process.stdout.write(`release endpoints verified at ${base}\n`);
